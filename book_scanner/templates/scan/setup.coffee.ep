# set up
app = app || {}

$ ->
  console.log( 'starting' )
  url = "<%= url_for '/scans/'.param('project')  %>"
  collection = new app.Project
    url: url
  new app.ProjectView
    collection: collection
